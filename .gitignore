# Compiled class files
*.class

# Log files
*.log

# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar

# Gradle (in case you ever migrate)
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IntelliJ IDEA
.idea/
*.iml
*.ipr
*.iws
out/

# Eclipse (I see you have .classpath and .project in your repo currently)
.classpath
.project
.settings/
bin/

# VS Code
.vscode/

# NetBeans
nbproject/private/
nbbuild/
dist/
nbdist/
.nb-gradle/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Temporary files
*.tmp
*.temp
*.swp
*~

# Lock files from your project (I saw one in your repo)
.~lock.*

# User-specific / private config (important for your tool!)
config.json
DebtorInformations.json

# Ad-hoc generated SEPA outputs
/payments-*.xml
/test.xml

# Generated artifacts in samples/ — only the curated input files + README are tracked
samples/**/*.xml
samples/**/*.html

# Design source assets (SVG / icon masters) — not shipped in the build
view/design/

# Build outputs
*.jar
!lib/*.jar
*.war
*.ear

# Logs and databases
*.log
*.sqlite

# Environment variables
.env
.env.local

# Local Copilot/agent instructions — kept locally, not published in the repo
.github/copilot-instructions.md

# Local maintainer/utility scripts — kept locally, not published in the repo
/downloads_count.ps1
/ico.ps1